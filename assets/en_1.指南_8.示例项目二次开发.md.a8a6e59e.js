import{_ as e,a as t,b as o,c as n,d as i,e as r,f as a,g as s,h as l,i as d,j as c,k as p,l as u,m as h}from"./chunks/image-14.a699b0c5.js";import{_ as m}from"./chunks/image-24.d043b60a.js";import{_ as f,o as g,c as y,V as b}from"./chunks/framework.24f0af87.js";const x="/mxcad_docs/assets/image-21.ee4fe07a.png",I=JSON.parse('{"title":"6. Example Project for Secondary Development","description":"","frontmatter":{},"headers":[],"relativePath":"en/1.指南/8.示例项目二次开发.md","filePath":"en/1.指南/8.示例项目二次开发.md"}'),w={name:"en/1.指南/8.示例项目二次开发.md"},v=b('<h1 id="_6-example-project-for-secondary-development" tabindex="-1">6. Example Project for Secondary Development <a class="header-anchor" href="#_6-example-project-for-secondary-development" aria-label="Permalink to &quot;6. Example Project for Secondary Development&quot;">​</a></h1><p>This project aims to facilitate secondary development of the example project implemented using mxcad. By configuring and using plugins, you can create your own drawing editing page.</p><p>The example project is built on Vue 3 and Vuetify 3 UI frameworks and the mxcad library.</p><div class="tip custom-block"><p class="custom-block-title">Recommendation</p><p>Embed the example project using an iframe for an efficient and non-intrusive way to integrate it into your project. With the provided plugin mechanism, you can meet various customization needs.</p></div><h2 id="h5-version" tabindex="-1">H5 Version <a class="header-anchor" href="#h5-version" aria-label="Permalink to &quot;H5 Version&quot;">​</a></h2><p>Demo URL: <a href="https://demo.mxdraw3d.com:3000/mxcad/" target="_blank" rel="noreferrer">https://demo.mxdraw3d.com:3000/mxcad/</a></p><p>Source code download location: <a href="https://demo.mxdraw3d.com:3562/MxCADCode.7z" target="_blank" rel="noreferrer">https://demo.mxdraw3d.com:3562/MxCADCode.7z</a></p><p>After extracting the directory, you will find the following folders:</p><ul><li><p>dist: Contains the frontend resources for the MxCad online editing project after building.</p></li><li><p>MxCAD: Contains plugins for extending the MxCad online editing project.</p></li><li><p>MxCADiframe: Contains a demo that embeds the MxCad online editing project using an iframe.</p></li></ul><p>First, navigate to the MxCAD and MxCADiframe directories, and run the <code>npm install</code> command.</p><p>To run and debug the MxCAD project, use the command: <code>npm run dev</code>.</p><p>Result after running: <img src="'+e+'" alt="Alt text"></p><p>To run and debug the MxCADiframe project, use the command: <code>npm run serve</code>.</p><p>Directly opening it may not retrieve the drawing or the iframe may display a &quot;localhost refused to connect&quot; error. To resolve this, visit <code>http://localhost:8081/?debug=true</code>. This is because it needs to be on the same origin. In other words, you should load the frontend resources from the dist directory on your own project&#39;s server with the same port. Then, embed the MxCad online editing project in your frontend using an iframe.</p><p>Result after running: <img src="'+t+'" alt="Alt text"></p><p>The entire process for secondary development of the MxCad online editing frontend project is as follows:</p><ol><li><p>Download the source code, unzip it, and navigate to the MxCAD directory. Run <code>npm install</code> to install dependencies.</p></li><li><p>Run <code>npm run dev</code> to debug and develop your own requirements.</p></li><li><p>Run <code>npm run build</code> to build the plugin. A JavaScript file will be generated in the dist/plugins directory.</p></li><li><p>Copy all the frontend resources in the dist directory to your own project&#39;s (debug) server.</p></li><li><p>In the MxCADiframe project, run <code>npm install</code> to install dependencies.</p></li><li><p>In the <code>src/components/Home.vue</code> file, set the <code>src</code> attribute of the iframe to the URL of the frontend resources you copied to your project&#39;s server.</p></li><li><p>Finally, if everything looks fine, follow the approach in the MxCADiframe directory to embed the MxCad online editing project in your own frontend project using an iframe.</p></li></ol><p>The above steps only cover what needs to be done on the frontend. In practice, you will need collaboration from the backend and implementation of necessary backend APIs, including features like uploading drawings and saving DWG files. Therefore, you need to download the MxDraw Cloud Development Package from <a href="https://www.mxdraw.com/download.html" target="_blank" rel="noreferrer">here</a> and follow the getting started guide to understand how to use it: <a href="https://help.mxdraw.com/?pid=32" target="_blank" rel="noreferrer">MxDraw Cloud Development Package Guide</a>.</p><p>You can directly copy all the files in the dist directory to <code>MxDrawCloudServer\\SRC\\TsWeb\\public\\mxcad</code>. This will replace all the existing files.</p><p>Then, double-click to run <code>Mx3dServer.exe</code>:</p><ol><li><p>Click &quot;开始web服务&quot; (Start Web Service).</p></li><li><p>Click &quot;启动MxCAD&quot; (Start MxCAD).</p></li></ol><p>You will see the complete modified version of the project. The buttons and the opened page in MxCADiframe are available in the MxCADCode.7z archive, which contains the source code. By following the steps above, you can achieve the frontend part of your secondary development. To implement services for uploading and saving drawings, you will need to refer to the MxDraw Cloud Development Package documentation.</p><p>Now, let&#39;s delve into the dist directory to understand how to achieve secondary development through configuration and plugins.</p><h3 id="understanding-the-dist-directory" tabindex="-1">Understanding the dist Directory <a class="header-anchor" href="#understanding-the-dist-directory" aria-label="Permalink to &quot;Understanding the dist Directory&quot;">​</a></h3><p>The dist directory contains the packaged frontend resources that can be deployed on your server.</p><p>The MxCAD project is started using a Node server for debugging, which serves the index.html file located in the dist directory. The MxCAD project&#39;s build with <code>npm run build</code> generates a test.js file (by default, a sample test plugin) in the dist/plugins directory.</p><p>Some important configuration files are:</p><p><strong>mxUiConfig.json</strong>:</p><p>The mxUiConfig.json file in the dist directory is used for configuring the user interface:</p><ul><li>title: The browser&#39;s title.</li></ul><p><img src="'+o+'" alt="Alt text"></p><ul><li>headerTitle: It automatically replaces <code>&lt;version&gt;</code> with the version number.</li></ul><p><img src="'+n+'" alt="Alt text"></p><ul><li>mTitleButtonBarData: An array of elements where &quot;prompt&quot; represents a tooltip and &quot;cmd&quot; represents a command. Clicking the button executes a command.</li></ul><p><img src="'+i+'" alt="Alt text"></p><ul><li>mRightButtonBarData and mLeftButtonBarData: &quot;isShow&quot; indicates whether to show the buttons.</li></ul><p><img src="'+r+'" alt="Alt text"></p><ul><li>mMenuBarData: A menu list. You can nest lists within lists to create multi-level menus.</li></ul><p><img src="'+a+'" alt="Alt text"></p><ul><li>footerRightBtnSwitchData: An array of button names to be displayed. An empty array means no buttons will be displayed.</li></ul><p><img src="'+s+`" alt="Alt text"></p><p>For more configurations, refer to the configuration file, which provides detailed explanations.</p><p><strong>mxServerConfig.json</strong>:</p><ul><li><p>uploadFileConfig: This configuration is based on <a href="http://fex.baidu.com/webuploader/" target="_blank" rel="noreferrer">WebUploader</a> for file uploads. It has several configuration parameters, and the backend upload API is explained below:</p><ul><li><p>baseUrl: The base URL for the same backend server. The relative interfaces below are all based on the same server address.</p></li><li><p>fileisExist: This interface returns data.ret === &quot;fileAlreadyExist&quot; if the file already exists, indicating a rapid upload through MD5 check. Otherwise, the file needs to be uploaded. This POST request carries two parameters: { fileHash: &quot;file MD5&quot;, filename: &quot;file name&quot; }. You can implement the corresponding backend logic according to your needs.</p></li><li><p>chunkisExist: This POST interface checks if a chunk already exists. It carries the following parameters:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#89DDFF;">{</span></span>
<span class="line"><span style="color:#A6ACCD;">  chunk</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">chunk</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">  chunks</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">total chunks</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">  fileName</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">file name</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">  fileHash</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">file MD5</span><span style="color:#89DDFF;">&quot;</span><span style="color:#89DDFF;">,</span></span>
<span class="line"><span style="color:#A6ACCD;">  size</span><span style="color:#89DDFF;">:</span><span style="color:#A6ACCD;"> </span><span style="color:#89DDFF;">&quot;</span><span style="color:#C3E88D;">chunk file size</span><span style="color:#89DDFF;">&quot;</span></span>
<span class="line"><span style="color:#89DDFF;">}</span></span></code></pre></div><p>The backend returns data.ret === &quot;chunkAlreadyExist&quot; if the chunk already exists; otherwise, it indicates that the chunk doesn&#39;t exist.</p></li><li><p>mxfilepath: Indicates the directory where uploaded drawing files are stored and converted to .mxweb file format. The files saved by the backend must follow this format: <code>fileHash.type.mxweb</code>, where:</p><ul><li><code>fileHash</code> is the MD5 value of the original CADfile.</li><li><code>type</code> is the original file extension of the CAD drawing.</li></ul></li><li><p>saveDwgUrl: This is the service address for saving DWG files. The implementation of this interface can be provided through the development package.</p></li></ul><p>Default location of the file-saving service in the Node server: Windows: <img src="`+l+'" alt="Alt text"> Linux: <img src="'+d+'" alt="Alt text"></p><ul><li>wasmConfig: This configuration distinguishes which wasm-related files to use. You can find more details in the configuration files in the dist directory.</li></ul></li></ul><p><strong>Plugin Configuration File - plugins/config.json</strong>:</p><p>The plugins folder contains files with the names of the plugins. They will be loaded one by one in the order specified in the plugins configuration in the <code>plugins/config.json</code> file. For example, if you have a <code>plugins/test.js</code> file, you should add &quot;test&quot; as an entry in the configuration.</p><p>The MxCAD directory is for creating the corresponding JavaScript files in dist/plugins. For instance:</p><p><img src="'+c+'" alt="Alt text"></p><p>MxCAD Directory Overview:</p><ul><li><p>Built on Vite, you can directly run and serve the dist directory by executing <code>npm run dev</code>. This allows you to view and modify .ts and .vue files, which will be automatically compiled and the page will refresh.</p></li><li><p>Vite requires manual execution of <code>npm run build</code> to bundle the dist directory. After bundling, the dist directory should be placed in dist/plugins.</p></li><li><p>When you use <code>import</code> to include mxcad, mxdraw, and Vue, you are referring to the bundled frontend resources in dist, not creating entirely new instances of mxcad, mxdraw, and Vue.</p></li><li><p>The Vite configuration in the MxCAD directory must match the plugins configuration in <code>vite.config.ts</code> and <code>dist/plugins/config.json</code>.</p></li></ul><p><strong>Tip</strong>: During secondary development, you can refer to the documentation of <a href="https://mxcadx.gitee.io/mxcad_docs/en/" target="_blank" rel="noreferrer">mxcad frontend library</a> and <a href="https://mxcadx.gitee.io/mxdraw_docs/" target="_blank" rel="noreferrer">mxdraw frontend library</a> for API references and introductory documents to implement your own requirements.</p><p>In the src directory of the MxCAD directory, you will find a multitude of test cases that demonstrate various functions implemented using the mxcad library. You can run them during debugging, either through the test buttons on the page or by entering commands in the command line.</p><p><img src="'+x+'" alt="Alt text"><img src="'+p+'" alt="Alt text"></p><p>The code corresponding to the functionality of a specific button can be found by searching for it in the source code.</p><p><img src="'+u+'" alt="Alt text"></p><p>After developing a plugin, run <code>npm run build</code> to bundle it into the dist/plugins directory.</p><p>Now, you just need to place the dist directory on your server and embed it using an iframe. The MxCADiframe project serves as a simple demo that you can refer to for integrating the dist frontend resources into your project.</p><p>In the src directory of the MxCAD directory, there&#39;s an iframe.ts file that corresponds to the postMessage in the MxCADiframe project.</p><p><img src="'+h+'" alt="Alt text"></p><p>We recommend using this method to implement your custom requirements and for integration and deployment.</p><p>With these steps, you can embed the dist frontend resources into your project and implement your own requirements through configuration and plugins.</p><p>If you have any further questions or need assistance with a specific aspect of this development process, feel free to ask!</p><h2 id="electron-version" tabindex="-1">Electron Version <a class="header-anchor" href="#electron-version" aria-label="Permalink to &quot;Electron Version&quot;">​</a></h2><p>We provide an Electron version for building desktop applications.</p><p>Download link: <a href="https://gitee.com/mxcadx/mxdraw-article/blob/master/MxCad%E9%A1%B9%E7%9B%AE%E4%BA%8C%E6%AC%A1%E5%BC%80%E5%8F%91%E6%8F%92%E4%BB%B6%E9%9B%86%E6%88%90/MxCADAppElectron.zip" target="_blank" rel="noreferrer">MxCADAppElectron.zip</a></p><p>After downloading and extracting it, you can set up the Electron project by running <code>npm install</code> to install the dependencies and then running <code>npm run dev</code> to start the Electron project.</p><p>For the Electron version, there are no changes in the frontend plugins for secondary development (the JavaScript files generated in the MxCAD directory mentioned above).</p><p>The only difference is that the Electron version on Windows has added an object called <code>MxElectronAPI</code>, which provides the ability to communicate with the main thread. You can use <code>MxElectronAPI</code> to determine if it&#39;s running in an Electron environment when developing frontend plugins.</p><p>In the Electron project, there are also plugins designed for specific requirements in the Electron main thread. These plugins are written in TypeScript and are bundled using Vite. The Vite configuration can be found in <code>vite.plugins.config.ts</code>.</p><p>Here are the steps to create a new Electron project plugin:</p><ol><li><p>Create a <code>src/plugins</code> directory if it doesn&#39;t already exist.</p></li><li><p>Inside the <code>src/plugins</code> directory, create a new directory for your plugin (e.g., <code>testPlugin</code>), and create an <code>index.ts</code> file as the entry point for your plugin.</p></li><li><p>Add the plugin entry configuration in <code>vite.plugins.config.ts</code> with <code>pluginEntryFileName: [&quot;plugins/testPlugin/index.ts&quot;]</code>.</p></li><li><p>Run the command for debugging: <code>dev:plugins</code>.</p></li><li><p>Run the command for building: <code>build:plugins</code>.</p></li><li><p>If you create a <code>preload.ts</code> in your <code>testPlugin</code> directory and export an object from it, this object will be used for communication with the web page. This is similar to the concept of a preload script, as described here: <a href="https://www.electronjs.org/zh/docs/latest/tutorial/tutorial-preload" target="_blank" rel="noreferrer">Electron Preload</a>.</p></li><li><p>Plugins can have many directories, and each directory represents a plugin. The directory name also serves as the namespace. In the frontend, you can access the plugin object using <code>window.MxElectronAPI.namespace</code>, where <code>namespace</code> is the name of the plugin directory.</p></li></ol><p>In Electron project plugins, the <code>global.mxAppContext</code> object is available. It provides access to several properties and methods, such as <code>getMainWindow</code>, <code>showMessage</code>, <code>showMessageBoxSync</code>, and <code>MainTabs</code>. You can access these through TypeScript with type hints and explanations.</p><p>Here&#39;s a description of the directories in the Electron project:</p><table><thead><tr><th>Path</th><th>Description</th><th>Note</th></tr></thead><tbody><tr><td>dist</td><td>Contains the frontend project resources</td><td>It has 2d and 3d directories, each with a <code>dist</code> directory. This <code>dist</code> directory is where you replace the frontend resources, just as mentioned above. Make sure to check if the <code>config.json</code> file has the necessary configurations.</td></tr><tr><td>dist-electron</td><td>Contains the main thread code for Electron</td><td>It&#39;s not recommended to modify this code directly, as it may be updated and replaced at any time.</td></tr><tr><td>rendererTypes</td><td>TypeScript type definitions for window.MxElectronAPI</td><td>These definitions and descriptions can be used in the frontend.</td></tr><tr><td>src/plugins</td><td>Plugin directories</td><td>Each directory contains an <code>index.ts</code> as the entry point.</td></tr><tr><td>vite.plugins.config.ts</td><td>Vite&#39;s build configuration</td><td>Every newly created directory should be documented here. If you are familiar with Vite, you can make adjustments based on your requirements, but ensure that the final output file structure remains unchanged, as any changes may prevent the loading of plugins.</td></tr></tbody></table><p>If you have specific secondary development requirements or if the existing configurations or plugins are insufficient, please provide feedback, and we will continue to improve them.</p><p>Screenshot of the Electron desktop application:</p><p><img src="'+m+'" alt="Alt text"></p>',77),D=[v];function A(C,_,T,j,k,M){return g(),y("div",null,D)}const P=f(w,[["render",A]]);export{I as __pageData,P as default};
